# Smart alarm
## Installation
#### Clone the repo
```shell
git clone https://github.com/Kalyna-Serhii/smart-alarm
```
#### Open project's root directory
```shell
cd smart-alarm
````
#### Build
```shell
docker build -t smart-alarm .
```
#### Run
```shell
docker run -d --name smart-alarm smart-alarm
```
